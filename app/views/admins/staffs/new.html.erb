<% @title = "職員新規登録" %>
<h1><%= @title %></h1>

<div id="generic-form">
  <%= form_with model: @staff, url: [ :admins, @staff ] do |f| %>
    <%= render "form", f: f %>
    <div class="buttons">
      <%= f.submit "登録" %>
      <%= link_to "キャンセル", :admins_staffs %>
    </div>
  <% end %>
</div>
